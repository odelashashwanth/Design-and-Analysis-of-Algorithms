# Max Heap Sort in Java

Implemented **Max Heap Sort** in Java using `Scanner` input. The program builds a Max Heap, sorts elements in ascending order, displays execution time, and includes complexity analysis.

**Complexity:**

* Best: O(n log n)
* Average: O(n log n)
* Worst: O(n log n)
* Space: O(1)
